const ADD = "app/selection/add"
const REMOVE = "app/selection/remove"
const ADD_ALL = "app/selection/add_all"
const CLEAR = "app/selection/clear"

export default {
  ADD,
  REMOVE,
  ADD_ALL,
  CLEAR
}
