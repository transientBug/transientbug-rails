import actions from "./actions"

const add = actions.add
const remove = actions.remove
const addAll = actions.addAll
const clear = actions.clear

export default {
  add,
  remove,
  addAll,
  clear
}
