export const TextWidget = ({ value, onChange }) => <input type="text" value={ value } onChange={ onChange } />
export const NumberWidget = ({ value, onChange }) => <input type="number" value={ value } onChange={ onChange } />
export const DateWidget = ({ value, onChange }) => <input type="date" value={ value } onChange={ onChange } />
