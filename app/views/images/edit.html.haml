- content_for :page_title do
  = @image.title

= render "navbar"

- content_for :body do
  .ui.fluid.grid.container
    .row
      %h1 Editing Image

    .row
      = image_tag @image.image, class: "ui top attached centered image"

    .row
      = render partial: "form", locals: { image: @image }
