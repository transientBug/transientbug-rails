- content_for :page_title do
  Image search

= render "navbar"

- content_for :body do
  .ui.fluid.grid
    .ui.centered.row
      %h1 Image Search Results

    = render partial: "cards", locals: { images: @images }
