= form_for @admin_application do |f|
  - if @admin_application.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_application.errors.count, "error")} prohibited this admin_application from being saved:"
      %ul
        - @admin_application.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
