.item
  .header OAuth Applications

.item
  .ui.transparent.icon.input
    = form_tag admin_applications_path, method: :get do
      = search_field_tag "q", params[:q], class: "ui prompt", placeholder: "Search ...", aria: { label: "search" }
    %i.search.link.icon

= active_link_to admin_applications_path, active: :exclusive, class: "item" do
  Index
  - if defined?(count) && count
    .ui.label= count

= content_for :menu
