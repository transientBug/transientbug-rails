<div class="ui modal">
  <div class="ui header">
    <i class="trash icon"></i>
    Disable Service Announcements?
  </div>
  <div class="scrolling content">
    <p>Are you sure you want to disable these service announcements?</p>
    <p>This will not delete them, only prevent them from showing for users.</p>
    <table class="ui celled table">
      <thead>
        <th>Title</th>
      </thead>
      <tbody>
        <% models.forEach((model) => { %>
          <tr>
            <td><%= model.title %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
  <div class="actions">
    <div class="ui green basic cancel inverted button">
      <i class="remove icon"></i>
      Cancel!
    </div>
    <div class="ui red ok inverted button">
      <i class="checkmark icon"></i>
      Yes, I'm sure
    </div>
  </div>
</div>
