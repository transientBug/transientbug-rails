<div class="ui modal">
  <div class="ui header">
    <i class="download icon"></i>
    Recache Bookmarks?
  </div>
  <div class="scrolling content">
    <p>This will cause all of the offline caches for the following bookmarks to be recached.</p>
    <p>This may take some time, depending on the number of bookmarks you are bulk recaching.</p>
    <table class="ui celled table">
      <thead>
        <th>Title</th>
        <th>URL</th>
      </thead>
      <tbody>
        <% models.forEach((model) => { %>
          <tr>
            <td><%= model.title %></td>
            <td><%= model.uri %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
  <div class="actions">
    <div class="ui cancel button">
      <i class="remove icon"></i>
      Cancel!
    </div>
    <div class="ui red ok button">
      <i class="checkmark icon"></i>
      DOEIT!
    </div>
  </div>
</div>
