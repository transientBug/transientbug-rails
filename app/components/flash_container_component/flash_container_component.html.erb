<div aria-live="assertive" class="fixed inset-0 flex items-end px-4 py-6 pointer-events-none sm:p-6 sm:items-start pointer-events-none z-50">
  <div class="w-full flex flex-col items-center space-y-4">
    <% types.each do |type| %>
      <%= render FlashComponent.new(message: @flash[type], type:) %>
    <% end %>
  </div>
</div>
